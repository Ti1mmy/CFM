export default function Loading() {

    return (
        <div className="inset-0 fixed bg-white flex justify-center items-center" style={{ fontFamily: 'var(--font-arcade)' }}>
            CFM
        </div>
    );
}